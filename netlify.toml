# netlify.toml
[functions]
  node_bundler = "esbuild"
  directory = "netlify/functions"

# Weekly refresh at 03:00 UTC every Monday
[[scheduled.functions]]
  function = "refresh"
  cron = "0 3 * * 1"
